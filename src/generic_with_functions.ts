//Generic Functions


//Apend in Ramba returns a new array of different types
//Any return true if at least one element fits the condition
/* 
//Format
export function append<T>(el: T, list: readonly T[]) : T[]; 
*/


//Function name is append
/* 
const updatedArray = append<string>('baz',["foo","bar"]);


const searchStr = "foo";
const hasSearchedString = any<string>((el: string) => el.contains(searchStr),[
    "foo",
    "bar",
    "bar",
]);
*/
